/* 

        Funciones en C++
        Ejercicio 16:

    Realice una funcion que dada una matriz
    y un numero de fila de la
    matriz devuelva el menor de los
    elementos almacenados en dicha fila.
 */
#include <iostream>
using std::cout; using std::cin; using std::endl;

// Funciones a ocupar:
void Ma();
void Add_M(int Ma[][50], int num_filas, int num_columnas);

int main()
{

    return 0;
}

void Ma()
{
    
}
void Add_M(int Ma[][50], int num_filas, int num_columnas)
{
    int *ptr = new int; // Reservacion de moeomria para in tipo de dato int.
    *ptr = 50; // Guardando el dato en la memoria reservada.

    // Ahora dinámicamente
   // int *ptr2 = new int Ma[50]; // Arreglo dinamico para enteros
     
}